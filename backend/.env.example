# backend/.env.example — DELETE this file once you copy the canonical .env.example
# from the repo root. Both files exist for convenience; the root one is the source of truth.
# See ../README.md and ../docs/AutoClock_DevDoc.md §3.

# Copy from the root .env.example:
#   cp ../.env.example .env
# then fill in real values. NEVER commit a populated .env.
