// SignInPage — STUB. Real implementation lands in commit D.

import { Link } from 'react-router-dom';

export default function SignInPage() {
  return (
    <div style={{ padding: 24 }}>
      <h1>Sign in (coming soon)</h1>
      <p>This will be the P01 Sign-in screen. Skip to <Link to="/onboarding">onboarding</Link> or <Link to="/today">today</Link>.</p>
    </div>
  );
}
